{
  "name": "todo-backend",
  "version": "1.0.0",
  "description": "Todo backend service",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "start:dev": "nodemon index.js",
    "build": "docker build -t jjairola/todo-backend:latest . && docker push jjairola/todo-backend:latest",
    "buildx": "docker buildx build --platform linux/amd64,linux/arm64 -t jjairola/todo-backend:latest --push .",
    "test": "jest"
  },
  "dependencies": {
    "express": "^4.18.2",
    "nats": "^2.28.2",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"

  }
}
