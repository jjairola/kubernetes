{
  "name": "todo-app",
  "version": "1.0.0",
  "description": "Deploy with `kubectl apply -a manifests`",
  "license": "ISC",
  "author": "",
  "type": "",
  "main": "index.js",
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "start": "node index.js",
    "start:dev": "nodemon index.js",
    "build": "docker build -t jjairola/webserver:latest . && docker push jjairola/webserver:latest",
    "buildx": "docker buildx build --platform linux/amd64,linux/arm64 -t jjairola/webserver:latest --push ."
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
